let numberOfStudent = 10 // infer from the value itself you can't assign other types of values in this variable

let randomNum = Math.random() > 0.5 ? 10 : 5;

let studentName : string = "Ranjit"; // same as let studentName = "Ranjit"

let isLoggedIn : boolean = false;

let isActive = true;
